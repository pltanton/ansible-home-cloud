---
- name: install docker packages
  apt: 
    package:
    - docker.io
    - python-setuptools
    - python-pip
    state: latest
    update_cache: yes

- name: install docker-py
  pip:
    name: docker
    version: 2.7.0
