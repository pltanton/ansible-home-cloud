- hosts: all
  user: root
  roles: 
  - common

- hosts: odroid1
  roles:
  - mosquitto
  - home-assistant
  - nginxproxy
  tasks:
  - hostname: { name: odroid1 }

- hosts: odroid2
  roles:
  - nextcloud
  tasks:
  - hostname: { name: odroid2 }
